Before writing code:

    cd IdeaProjects/Ex6/
    git fetch
    git pull origin master


Commit inside IntelliJ. Push also inside IntelliJ. On the command line:

    git add --all
    git commit -m "My message here"
    git push origin master

**Always run**:

    git status
